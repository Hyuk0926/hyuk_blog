# 운영 환경 설정
spring.profiles.active=prod

# 서버 포트 설정 (AWS Elastic Beanstalk 환경 변수 사용)
server.port=5000

# AWS RDS Database 설정 (실제 값 사용)
spring.datasource.url=jdbc:mysql://awseb-e-eipj6ckd2w-stack-awsebrdsdatabase-m0ru059s7ksh.cz86ig6e2h64.ap-northeast-2.rds.amazonaws.com:3306/hyuk_blog_db?useSSL=true&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8mb4&useUnicode=true
spring.datasource.username=admin
spring.datasource.password=Shka991204!
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Flyway 설정 (기존 데이터 보호)
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.out-of-order=true
spring.flyway.baseline-version=0
spring.flyway.baseline-description=Initial baseline for existing data

# 개인정보 표시 설정 (운영 환경에서는 주소와 전화번호 숨김)
app.show.address=false
app.show.phone=false

# 운영 환경에서는 캐시 활성화
spring.thymeleaf.cache=true

# 파일 업로드 설정
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB 